name: Build unity_fps_limit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Configure
        run: cmake -S src/unity_fps_limit -B build/unity_fps_limit -A x64

      - name: Build
        run: cmake --build build/unity_fps_limit --config Release --parallel

      - name: Install distributable files
        run: cmake --install build/unity_fps_limit --config Release --prefix dist/unity_fps_limit

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: unity_fps_limit-windows-x64
          path: dist/unity_fps_limit
          if-no-files-found: error
